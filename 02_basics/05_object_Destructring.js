const course = {
    coursename: 'JavaScript',
    duration: '3 months',
    price: "9990"
}

const {duration : dr} = course; // destructuring the object
console.log(dr);

// Apna kaaam kisi or ke uper daal dena = API
